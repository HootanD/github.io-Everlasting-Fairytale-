#! tempo-tempo-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: tempo-tempo-data
    namespace: everlasting-fairytale
    labels:
        com.docker.compose.service: tempo
        com.docker.compose.volume: tempo-data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
